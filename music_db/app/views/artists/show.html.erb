<p>
  <strong>Genre:</strong>
  <%= @artist.genre.name %>
</p>

<p>
  <strong>Songs by <%= @artist.name.capitalize %>:</strong>
  <ul>
    <% @artist.songs.order('name').each do |song| %>
    <li><% song.name %></li>
    <%end%>
  </ul>
</p>
</p>


<%=f.submit class: "btn-btn-info"%>
<%= link_to 'Edit', edit_artist_path(@artist) %> |
<%= link_to 'Back', artists_path %>
